"""
BaseStore — 存储抽象接口。

----------------------------------------
职责
----------------------------------------

定义统一持久化接口：

    save(key, obj)
    load(key)
    delete(key)
    list_keys()

----------------------------------------
重要原则
----------------------------------------

- 不包含数据库实现
- 不包含业务逻辑
- 不依赖 execution
- 不依赖 domain

----------------------------------------
目标
----------------------------------------

确保所有存储实现可替换。
"""