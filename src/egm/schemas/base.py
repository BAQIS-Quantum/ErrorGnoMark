"""
base.py

定义所有 Schema 的基础抽象类。

作用：
    - 提供统一的序列化接口
    - 提供版本控制字段
    - 提供 metadata 结构
    - 保证所有 Schema 可持久化

设计目标：
    - 可 JSON 序列化
    - 可存储到 datastore
    - 可跨模块传输

禁止：
    - 不包含实验逻辑
    - 不包含推理逻辑
"""