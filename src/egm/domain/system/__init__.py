"""
System Domain Layer.

This module defines the runtime hardware abstraction layer that bridges
profiling results, calibration updates, and compilation strategies.

It provides structured representations of device state, topology,
and cost interfaces without embedding physical error modeling logic.
"""