# `scripts/db/`（遗留）

Phase 1 的 **apply / verify / run SQL** 已迁至 **`../postgres/`**。

- 请使用：`scripts/postgres/apply_phase1.sh`、`verify_seed_counts.py`、`run_p0_queries.py`  
- 本目录若仍含 `phase1_database_checks.ipynb`，其中说明与 `%load` 路径应已改为指向 `scripts/postgres/`。
