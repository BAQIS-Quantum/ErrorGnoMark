# 先打通 execution-形成一个“可运行的 v3 baseline”
需要先部署连通好claude code;继续干活


# what to do next:

        我现在已经在* v3_integration 建构了 
        1. v3 新的文件结构；
        2. 实习生wuqingyuan 代码合并进v3结构，但是 execution 部分还未更新打通；
        3. 实习生fan的代码涉及的两个文件，我直接手动拖进来了， execution 部分还未更新打通；
        4. 最后一位实习生开发的 就是一个py 文件，我也拖进来了，execution 部分还未更新打通； 
        请问根据根据你之前给我的egm 工程治理升级秩序，现在，我应该 先在当前版本打通execution ，然后继续推进呢？还是要怎么样呢？


        ✅ 先打通 execution
        ✅ 形成一个“可运行的 v3 baseline”
        ✅ 再继续功能推进

不要再加新东西。

# 重要信息：

develop = 真实时间线
main = 稳定发布
feature/* = 实习生
exp/* = 实验协作

# kerui-开发涉及的模块：

from egm.core.backends.convert_egm2sisq import SISQBackend, egm_to_sisq

- backends.convert_egm2sisq
- 上次讨论重要问题：

        “​​2. 线路的“分解”与“编译”逻辑​​
        ​​当前实现​​: EGM内置了一套简易的“分解”逻辑，即将逻辑门（如U3）转换为特定硬件平台（如IBM、Google）的原生物理门（如旋转门）。这套逻辑旨在覆盖主流平台，但并非专业的编译优化。
        ​​未来方向​​: 讨论了两种未来的演进路径：

        ​​兼容性​​: 保留EGM的简易分解逻辑，同时提供一个可选的接口，允许用户接入更强大的第三方编译器（如宏泽老师的研究），以实现更优的编译、优化和降噪。
        ​​接口设计​​: 计划将“是否启用编译优化”作为参数，与“是否指定原生门”参数解耦，以支持从纯逻辑门到原生门再到高级编译优化的多种工作模式。”






# fan xingyan-开发涉及的模块：

    from egm.core.backends.Real_Quark_Backend import RealQuarkBackend
    from egm.suites.job_submit_fetch import JobSubmitFetch
    from egm.suites.campaign_store_sqlite import CampaignSQLiteStore

    - backends.Real_Quark_Backend
    - suites

    之后需要重新整合一下 execution/backends/RealQuarkBackend 这些-会尽量往后，就是最新开发进行兼容；当然如果必要也会往前修改；




# wuqingyuan 
        ✅ 完成按照 v3 结构重构egm
        - 【wuqingyuan + v3 已经完成】git commit -m "Refactor: move benchmarking and characterizationinto physical layer"- g
        - 需要测试适配新结构
        - 单元测试
        - fan 的，要不只是复制特定代码即可；现在他们开发的代码基础不一样；合并有问题/错误；

